{
  "name": "dma-conf-tool",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "node tools/web-server.js",
    "test": "node --test tests/*.test.js",
    "sweep": "node tools/sweep-extractor.js --concurrency 24"
  }
}
